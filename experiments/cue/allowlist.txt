# Resources permitted to differ between kustomize and CUE.
#
#   <bundle>  <kind>/<name>  <reason>
#
# An entry whose resource turns out to be identical, or which names a resource
# neither side emits, fails the gate: a stale exemption is a blind spot.
#
# Generator hash suffixes are NOT listed here. They are normalized away, since
# they are a systematic encoding difference rather than a fidelity problem.

# Each line below names a resource CUE renders per the fleet's own conventions
# while kustomize still renders a divergence, so every one dies with apps/base/.
audiobookshelf                  HelmRelease/audiobookshelf                     UMASK quoted, so it is a string like the rest of the fleet, and valuesFrom dropped
blocky                          HelmRelease/blocky                             metallb.io annotation domain, which supersedes metallb.universe.tf
dedicated-server-abiotic-factor HelmRelease/dedicated-server-abiotic-factor    gains the ReplicationDestination and dataSourceRef a restore needs
gotify                          HelmRelease/gotify                             backup switched on, and the volume gains its dataSourceRef
jellyfin                        HelmRelease/jellyfin                           metallb.io annotation domain, which supersedes metallb.universe.tf, and valuesFrom dropped
jellyseerr                      HelmRelease/jellyseerr                         valuesFrom dropped
kanidm                          HelmRelease/kanidm                             valuesFrom dropped
komga                           HelmRelease/komga                              UMASK quoted, and the volume gains its dataSourceRef
kube-prometheus-stack           HelmRelease/kube-prometheus-stack              doubled TLS host removed from the grafana and prometheus ingresses
loki                            HelmRelease/loki                               credentials moved onto environment the pods read, and valuesFrom dropped
openaudible                     HelmRelease/openaudible                        UMASK quoted, both volsync movers take the same security context, and valuesFrom dropped
pgadmin                         HelmRelease/pgadmin                            password moved onto the chart's own existingSecret, and valuesFrom dropped
qbittorrent                     HelmRelease/qbittorrent                        ingress moved onto chain-network-internal-whitelist, and valuesFrom dropped
traefik                         HelmRelease/traefik                            metallb.io annotation domain, which supersedes metallb.universe.tf
traefik                         HelmRelease/traefik-geo-lookup                 release deleted; nothing referenced it and geoblock calls get.geojs.io
traefik                         Secret/traefik-geo-lookup                      deleted with the release it configured
traefik                         HelmRepository/bjw-s                           deleted with traefik-geo-lookup, the only app-template release here

# Eight workloads took secret material from an encrypted values blob that
# spec.valuesFrom merged into the release. CUE ships each one as a Secret
# manifest instead, which the two sides cannot agree on: the blob has no
# counterpart, the Secrets have no golden document, and the HelmRelease loses
# the valuesFrom that pointed at the blob.
audiobookshelf                  Secret/audiobookshelf-restic-config            restic credential, now a manifest the backup declares
audiobookshelf                  Secret/audiobookshelf-restic-metadata          restic credential, now a manifest the backup declares
audiobookshelf                  Secret/audiobookshelf-secret-values-4mbfdcgm69 the values blob those credentials left
jellyfin                        Secret/jellyfin-restic-config                  restic credential, now a manifest the backup declares
jellyfin                        Secret/jellyfin-secret-values-mbk84tfmbk       the values blob that credential left
jellyseerr                      Secret/jellyseerr-restic-config                restic credential, now a manifest the backup declares
jellyseerr                      Secret/jellyseerr-secret-values-mhbt8d8tgd     the values blob that credential left
kanidm                          Secret/kanidm-restic-data                      restic credential, now a manifest the backup declares
kanidm                          Secret/kanidm-secret-values-g789cmfgch         the values blob that credential left
loki                            Secret/loki-s3                                 the garage credentials, which used to be expanded into the chart's ConfigMap
loki                            Secret/loki-secret-values-6gkm4t794k           the values blob those credentials left
openaudible                     Secret/openaudible-restic-config               restic credential, now a manifest the backup declares
openaudible                     Secret/openaudible-secret-values-9c64ctmtmc    the values blob that credential left
pgadmin                         Secret/pgadmin-credentials                     the admin password, which the chart used to generate from the blob
pgadmin                         Secret/pgadmin-secret-values-8m77gd6g59        the values blob that credential left
qbittorrent                     Secret/qbittorrent-restic-config               restic credential, now a manifest the backup declares
qbittorrent                     Secret/qbittorrent-secret-values-4bf884df2k    the values blob those credentials left
qbittorrent                     Secret/qbittorrent-vpn-config                  gluetun credential, moved out with the restic one
