name: nix-flake-check

on:
  workflow_dispatch:
  pull_request:
    branches:
      - main

permissions:
  contents: read

jobs:
  nix-flake-check:
    uses: Joker9944/nix-config/.github/workflows/nix-flake-check.yaml@main
