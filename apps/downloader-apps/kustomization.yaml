apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace.yaml
  - openaudible/helm-release.yaml
  - qbittorrent
secretGenerator:
  - name: openaudible-restic-values
    namespace: ${main_namespace}
    files:
      - values.yaml=openaudible/restic.values.sops.yaml
configurations:
  - ../../shared/kustomizeconfig.yaml
