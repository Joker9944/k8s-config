---
apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: home-assistant-cnpg
  namespace: ${main_namespace}
spec:
  description: PostgreSQL Cluster for home-assistant
  instances: 3
  storage:
    size: 1Gi
  bootstrap:
    initdb:
      database: home-assistant-recorder
      owner: home-assistant
