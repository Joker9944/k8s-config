services:

  ocm:
    persistence:
      enabled: true
      size: 1Gi

  search:
    persistence:
      enabled: true
      size: 1Gi

  storagesystem:
    persistence:
      enabled: true
      size: 1Gi

  storageusers:
    persistence:
      enabled: true

  thumbnails:
    persistence:
      enabled: true
      size: 1Gi
