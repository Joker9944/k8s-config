services:

  ocm:
    persistence:
      enabled: true

  search:
    persistence:
      enabled: true

  storagesystem:
    persistence:
      enabled: true

  storageusers:
    persistence:
      enabled: false

  thumbnails:
    persistence:
      enabled: true
