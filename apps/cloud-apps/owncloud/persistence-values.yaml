services:

  ocm:
    persistence:
      enabled: false

  search:
    persistence:
      enabled: false

  storagesystem:
    persistence:
      enabled: false

  storageusers:
    persistence:
      enabled: false

  thumbnails:
    persistence:
      enabled: false
