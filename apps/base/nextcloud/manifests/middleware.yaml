apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: caldav-carddav-redirect
spec:
  redirectRegex:
    permanent: true
    regex: https://(.*)/.well-known/(card|cal)dav
    replacement: https://${1}/remote.php/dav/
