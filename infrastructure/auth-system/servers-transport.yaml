apiVersion: traefik.io/v1alpha1
kind: ServersTransport
metadata:
  name: root-ca
  namespace: ${main_namespace}
spec:
  serverName: kanidm.${main_namespace}
  rootCAsSecrets: [ "wildcard-${main_namespace}-cert" ]
