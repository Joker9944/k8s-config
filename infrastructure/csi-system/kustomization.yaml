apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace.yaml
  - freenas-api-iscsi/helm-release.yaml
secretGenerator:
  - name: freenas-api-iscsi-values-secret
    namespace: csi-system
    files:
      - values.yaml=freenas-api-iscsi/values.sops.yaml
configurations:
  - kustomizeconfig.yaml
